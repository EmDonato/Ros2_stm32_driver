services:
  stm32_driver:
    image: open-pi-stm32-driver-img
    platform: ${ROBOT_PLATFORM:-linux/arm64}
    container_name: open-pi-stm32-driver
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USERNAME: stm32
        UID: ${HOST_UID:-1000}
        GID: ${HOST_GID:-1000}
      additional_contexts:
        interfaces: ${INTERFACES_CONTEXT:-../../interfaces}
    network_mode: host
    privileged: true
    volumes:
      - /dev:/dev
      - /run/udev:/run/udev:ro
      - ./config:/ws/config:ro
    environment:
      RMW_IMPLEMENTATION: rmw_cyclonedds_cpp
      ROS_DOMAIN_ID: ${ROS_DOMAIN_ID:-0}
